{
    "version": 1,
    "configurePresets": [
        {
            "name": "default",
            "generator": "Unix Makefiles",
            "binaryDir": "${sourceDir}/cmake-build"
        }
    ]
}
